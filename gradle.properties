# Where the ABI schema lives. There is no shared schema repository yet, so this
# points at a sibling GoCraft checkout; deliverable 01 gives abi/v1 its own
# repository and this becomes a real dependency instead of a path.
#
# Only `generateProto` reads it. A plain build needs nothing here: the generated
# sources are committed.
abiSchema=../GoCraft
