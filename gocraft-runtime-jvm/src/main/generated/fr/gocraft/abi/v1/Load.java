// Generated by the protocol buffer compiler.  DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: abi/v1/envelope.proto
// Protobuf Java Version: 4.36.0

package fr.gocraft.abi.v1;

/**
 * <pre>
 * Load asks the runtime to bring up one plugin from an already validated
 * bundle. The host has read the manifest; the runtime is told where the bundle
 * is, not asked to discover it.
 * </pre>
 *
 * Protobuf type {@code gocraft.abi.v1.Load}
 */
@com.google.protobuf.Generated
public  final class Load extends
    com.google.protobuf.GeneratedMessageLite<
        Load, Load.Builder> implements
    // @@protoc_insertion_point(message_implements:gocraft.abi.v1.Load)
    LoadOrBuilder {
  private Load() {
    pluginId_ = "";
    bundlePath_ = "";
    entry_ = "";
    dataDirectory_ = "";
    commandTree_ = "";
    eventTypes_ = emptyProtobufList();
  }
  public static final int PLUGIN_ID_FIELD_NUMBER = 1;
  private java.lang.String pluginId_;
  /**
   * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
   * @return The pluginId.
   */
  @java.lang.Override
  public java.lang.String getPluginId() {
    return pluginId_;
  }
  /**
   * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
   * @return The bytes for pluginId.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getPluginIdBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(pluginId_);
  }
  /**
   * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
   * @param value The pluginId to set.
   */
  private void setPluginId(
      java.lang.String value) {
    java.util.Objects.requireNonNull(value);

    pluginId_ = value;
  }
  /**
   * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
   */
  private void clearPluginId() {

    pluginId_ = getDefaultInstance().getPluginId();
  }
  /**
   * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
   * @param value The bytes for pluginId to set.
   */
  private void setPluginIdBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    pluginId_ = value.toStringUtf8();

  }

  public static final int BUNDLE_PATH_FIELD_NUMBER = 2;
  private java.lang.String bundlePath_;
  /**
   * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
   * @return The bundlePath.
   */
  @java.lang.Override
  public java.lang.String getBundlePath() {
    return bundlePath_;
  }
  /**
   * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
   * @return The bytes for bundlePath.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getBundlePathBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(bundlePath_);
  }
  /**
   * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
   * @param value The bundlePath to set.
   */
  private void setBundlePath(
      java.lang.String value) {
    java.util.Objects.requireNonNull(value);

    bundlePath_ = value;
  }
  /**
   * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
   */
  private void clearBundlePath() {

    bundlePath_ = getDefaultInstance().getBundlePath();
  }
  /**
   * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
   * @param value The bytes for bundlePath to set.
   */
  private void setBundlePathBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    bundlePath_ = value.toStringUtf8();

  }

  public static final int ENTRY_FIELD_NUMBER = 3;
  private java.lang.String entry_;
  /**
   * <code>string entry = 3 [json_name = "entry"];</code>
   * @return The entry.
   */
  @java.lang.Override
  public java.lang.String getEntry() {
    return entry_;
  }
  /**
   * <code>string entry = 3 [json_name = "entry"];</code>
   * @return The bytes for entry.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getEntryBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(entry_);
  }
  /**
   * <code>string entry = 3 [json_name = "entry"];</code>
   * @param value The entry to set.
   */
  private void setEntry(
      java.lang.String value) {
    java.util.Objects.requireNonNull(value);

    entry_ = value;
  }
  /**
   * <code>string entry = 3 [json_name = "entry"];</code>
   */
  private void clearEntry() {

    entry_ = getDefaultInstance().getEntry();
  }
  /**
   * <code>string entry = 3 [json_name = "entry"];</code>
   * @param value The bytes for entry to set.
   */
  private void setEntryBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    entry_ = value.toStringUtf8();

  }

  public static final int DATA_DIRECTORY_FIELD_NUMBER = 4;
  private java.lang.String dataDirectory_;
  /**
   * <pre>
   * Where this plugin's own files live: its configuration, unpacked from the
   * bundle's config/ on first load, and anything it writes for itself.
   *
   * The host owns it. It creates the directory, seeds the defaults and never
   * overwrites what an admin edited, so a runtime is told the path rather than
   * deriving one — two runtimes deriving it separately would eventually derive
   * it differently.
   *
   * Added in ABI 1 after the fact: a runtime built before this reads an empty
   * string, which is why nothing may assume it is set.
   * </pre>
   *
   * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
   * @return The dataDirectory.
   */
  @java.lang.Override
  public java.lang.String getDataDirectory() {
    return dataDirectory_;
  }
  /**
   * <pre>
   * Where this plugin's own files live: its configuration, unpacked from the
   * bundle's config/ on first load, and anything it writes for itself.
   *
   * The host owns it. It creates the directory, seeds the defaults and never
   * overwrites what an admin edited, so a runtime is told the path rather than
   * deriving one — two runtimes deriving it separately would eventually derive
   * it differently.
   *
   * Added in ABI 1 after the fact: a runtime built before this reads an empty
   * string, which is why nothing may assume it is set.
   * </pre>
   *
   * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
   * @return The bytes for dataDirectory.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getDataDirectoryBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(dataDirectory_);
  }
  /**
   * <pre>
   * Where this plugin's own files live: its configuration, unpacked from the
   * bundle's config/ on first load, and anything it writes for itself.
   *
   * The host owns it. It creates the directory, seeds the defaults and never
   * overwrites what an admin edited, so a runtime is told the path rather than
   * deriving one — two runtimes deriving it separately would eventually derive
   * it differently.
   *
   * Added in ABI 1 after the fact: a runtime built before this reads an empty
   * string, which is why nothing may assume it is set.
   * </pre>
   *
   * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
   * @param value The dataDirectory to set.
   */
  private void setDataDirectory(
      java.lang.String value) {
    java.util.Objects.requireNonNull(value);

    dataDirectory_ = value;
  }
  /**
   * <pre>
   * Where this plugin's own files live: its configuration, unpacked from the
   * bundle's config/ on first load, and anything it writes for itself.
   *
   * The host owns it. It creates the directory, seeds the defaults and never
   * overwrites what an admin edited, so a runtime is told the path rather than
   * deriving one — two runtimes deriving it separately would eventually derive
   * it differently.
   *
   * Added in ABI 1 after the fact: a runtime built before this reads an empty
   * string, which is why nothing may assume it is set.
   * </pre>
   *
   * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
   */
  private void clearDataDirectory() {

    dataDirectory_ = getDefaultInstance().getDataDirectory();
  }
  /**
   * <pre>
   * Where this plugin's own files live: its configuration, unpacked from the
   * bundle's config/ on first load, and anything it writes for itself.
   *
   * The host owns it. It creates the directory, seeds the defaults and never
   * overwrites what an admin edited, so a runtime is told the path rather than
   * deriving one — two runtimes deriving it separately would eventually derive
   * it differently.
   *
   * Added in ABI 1 after the fact: a runtime built before this reads an empty
   * string, which is why nothing may assume it is set.
   * </pre>
   *
   * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
   * @param value The bytes for dataDirectory to set.
   */
  private void setDataDirectoryBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    dataDirectory_ = value.toStringUtf8();

  }

  public static final int COMMAND_TREE_FIELD_NUMBER = 5;
  private java.lang.String commandTree_;
  /**
   * <pre>
   * Where the command tree sits inside the bundle, from the manifest's
   * [commands] tree. Empty when the plugin declares no commands.
   *
   * The path, not the tree. The runtime opens this bundle anyway, and the tree
   * is what its executor ids are numbered by, so sending a copy would be a
   * second definition of it — free to disagree with the one the runtime binds
   * its handlers against. Sending the path instead is what the host already
   * does for entry: it read the manifest, so the runtime does not have to.
   * </pre>
   *
   * <code>string command_tree = 5 [json_name = "commandTree"];</code>
   * @return The commandTree.
   */
  @java.lang.Override
  public java.lang.String getCommandTree() {
    return commandTree_;
  }
  /**
   * <pre>
   * Where the command tree sits inside the bundle, from the manifest's
   * [commands] tree. Empty when the plugin declares no commands.
   *
   * The path, not the tree. The runtime opens this bundle anyway, and the tree
   * is what its executor ids are numbered by, so sending a copy would be a
   * second definition of it — free to disagree with the one the runtime binds
   * its handlers against. Sending the path instead is what the host already
   * does for entry: it read the manifest, so the runtime does not have to.
   * </pre>
   *
   * <code>string command_tree = 5 [json_name = "commandTree"];</code>
   * @return The bytes for commandTree.
   */
  @java.lang.Override
  public com.google.protobuf.ByteString
      getCommandTreeBytes() {
    return com.google.protobuf.ByteString.copyFromUtf8(commandTree_);
  }
  /**
   * <pre>
   * Where the command tree sits inside the bundle, from the manifest's
   * [commands] tree. Empty when the plugin declares no commands.
   *
   * The path, not the tree. The runtime opens this bundle anyway, and the tree
   * is what its executor ids are numbered by, so sending a copy would be a
   * second definition of it — free to disagree with the one the runtime binds
   * its handlers against. Sending the path instead is what the host already
   * does for entry: it read the manifest, so the runtime does not have to.
   * </pre>
   *
   * <code>string command_tree = 5 [json_name = "commandTree"];</code>
   * @param value The commandTree to set.
   */
  private void setCommandTree(
      java.lang.String value) {
    java.util.Objects.requireNonNull(value);

    commandTree_ = value;
  }
  /**
   * <pre>
   * Where the command tree sits inside the bundle, from the manifest's
   * [commands] tree. Empty when the plugin declares no commands.
   *
   * The path, not the tree. The runtime opens this bundle anyway, and the tree
   * is what its executor ids are numbered by, so sending a copy would be a
   * second definition of it — free to disagree with the one the runtime binds
   * its handlers against. Sending the path instead is what the host already
   * does for entry: it read the manifest, so the runtime does not have to.
   * </pre>
   *
   * <code>string command_tree = 5 [json_name = "commandTree"];</code>
   */
  private void clearCommandTree() {

    commandTree_ = getDefaultInstance().getCommandTree();
  }
  /**
   * <pre>
   * Where the command tree sits inside the bundle, from the manifest's
   * [commands] tree. Empty when the plugin declares no commands.
   *
   * The path, not the tree. The runtime opens this bundle anyway, and the tree
   * is what its executor ids are numbered by, so sending a copy would be a
   * second definition of it — free to disagree with the one the runtime binds
   * its handlers against. Sending the path instead is what the host already
   * does for entry: it read the manifest, so the runtime does not have to.
   * </pre>
   *
   * <code>string command_tree = 5 [json_name = "commandTree"];</code>
   * @param value The bytes for commandTree to set.
   */
  private void setCommandTreeBytes(
      com.google.protobuf.ByteString value) {
    checkByteStringIsUtf8(value);
    commandTree_ = value.toStringUtf8();

  }

  public static final int EVENT_TYPES_FIELD_NUMBER = 6;
  private com.google.protobuf.Internal.ProtobufList<fr.gocraft.abi.v1.EventBinding> eventTypes_;
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  @java.lang.Override
  public java.util.List<fr.gocraft.abi.v1.EventBinding> getEventTypesList() {
    return eventTypes_;
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  public java.util.List<? extends fr.gocraft.abi.v1.EventBindingOrBuilder> 
      getEventTypesOrBuilderList() {
    return eventTypes_;
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  @java.lang.Override
  public int getEventTypesCount() {
    return eventTypes_.size();
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  @java.lang.Override
  public fr.gocraft.abi.v1.EventBinding getEventTypes(int index) {
    return eventTypes_.get(index);
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  public fr.gocraft.abi.v1.EventBindingOrBuilder getEventTypesOrBuilder(
      int index) {
    return eventTypes_.get(index);
  }
  private void ensureEventTypesIsMutable() {
    com.google.protobuf.Internal.ProtobufList<fr.gocraft.abi.v1.EventBinding> tmp = eventTypes_;
    if (!tmp.isModifiable()) {
      eventTypes_ =
          com.google.protobuf.GeneratedMessageLite.mutableCopy(tmp);
     }
  }

  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  private void setEventTypes(
      int index, fr.gocraft.abi.v1.EventBinding value) {
    java.util.Objects.requireNonNull(value);
    ensureEventTypesIsMutable();
    eventTypes_.set(index, value);
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  private void addEventTypes(fr.gocraft.abi.v1.EventBinding value) {
    java.util.Objects.requireNonNull(value);
    ensureEventTypesIsMutable();
    eventTypes_.add(value);
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  private void addEventTypes(
      int index, fr.gocraft.abi.v1.EventBinding value) {
    java.util.Objects.requireNonNull(value);
    ensureEventTypesIsMutable();
    eventTypes_.add(index, value);
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  private void addAllEventTypes(
      java.lang.Iterable<? extends fr.gocraft.abi.v1.EventBinding> values) {
    ensureEventTypesIsMutable();
    com.google.protobuf.AbstractMessageLite.addAll(
        values, eventTypes_);
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  private void clearEventTypes() {
    eventTypes_ = emptyProtobufList();
  }
  /**
   * <pre>
   * The plugin-defined event types this plugin provides or subscribes to, with
   * the id the host assigned each of them. Empty for a plugin that touches
   * none, which is every plugin until one declares [[events.provides]].
   *
   * Sent per plugin rather than once per runtime, because a runtime has no use
   * for the id of an event none of its plugins can see, and because the host
   * assigns the ids from the full set of installed manifests — a fact that is
   * only settled once every bundle has been scanned.
   * </pre>
   *
   * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
   */
  private void removeEventTypes(int index) {
    ensureEventTypesIsMutable();
    eventTypes_.remove(index);
  }

  public static fr.gocraft.abi.v1.Load parseFrom(
      java.nio.ByteBuffer data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(
      java.nio.ByteBuffer data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data, extensionRegistry);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(
      com.google.protobuf.ByteString data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(
      com.google.protobuf.ByteString data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data, extensionRegistry);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(byte[] data)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(
      byte[] data,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws com.google.protobuf.InvalidProtocolBufferException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, data, extensionRegistry);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(java.io.InputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, input);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, input, extensionRegistry);
  }

  public static fr.gocraft.abi.v1.Load parseDelimitedFrom(java.io.InputStream input)
      throws java.io.IOException {
    return parseDelimitedFrom(DEFAULT_INSTANCE, input);
  }

  public static fr.gocraft.abi.v1.Load parseDelimitedFrom(
      java.io.InputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return parseDelimitedFrom(DEFAULT_INSTANCE, input, extensionRegistry);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(
      com.google.protobuf.CodedInputStream input)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, input);
  }
  public static fr.gocraft.abi.v1.Load parseFrom(
      com.google.protobuf.CodedInputStream input,
      com.google.protobuf.ExtensionRegistryLite extensionRegistry)
      throws java.io.IOException {
    return com.google.protobuf.GeneratedMessageLite.parseFrom(
        DEFAULT_INSTANCE, input, extensionRegistry);
  }

  public static Builder newBuilder() {
    return (Builder) DEFAULT_INSTANCE.createBuilder();
  }
  public static Builder newBuilder(fr.gocraft.abi.v1.Load prototype) {
    return DEFAULT_INSTANCE.createBuilder(prototype);
  }

  /**
   * <pre>
   * Load asks the runtime to bring up one plugin from an already validated
   * bundle. The host has read the manifest; the runtime is told where the bundle
   * is, not asked to discover it.
   * </pre>
   *
   * Protobuf type {@code gocraft.abi.v1.Load}
   */
  public static final class Builder extends
      com.google.protobuf.GeneratedMessageLite.Builder<
        fr.gocraft.abi.v1.Load, Builder> implements
      // @@protoc_insertion_point(builder_implements:gocraft.abi.v1.Load)
      fr.gocraft.abi.v1.LoadOrBuilder {
    // Construct using fr.gocraft.abi.v1.Load.newBuilder()
    private Builder() {
      super(DEFAULT_INSTANCE);
    }


    /**
     * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
     * @return The pluginId.
     */
    @java.lang.Override
    public java.lang.String getPluginId() {
      return instance.getPluginId();
    }
    /**
     * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
     * @return The bytes for pluginId.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getPluginIdBytes() {
      return instance.getPluginIdBytes();
    }
    /**
     * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
     * @param value The pluginId to set.
     * @return This builder for chaining.
     */
    public Builder setPluginId(
        java.lang.String value) {
      copyOnWrite();
      instance.setPluginId(value);
      return this;
    }
    /**
     * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
     * @return This builder for chaining.
     */
    public Builder clearPluginId() {
      copyOnWrite();
      instance.clearPluginId();
      return this;
    }
    /**
     * <code>string plugin_id = 1 [json_name = "pluginId"];</code>
     * @param value The bytes for pluginId to set.
     * @return This builder for chaining.
     */
    public Builder setPluginIdBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setPluginIdBytes(value);
      return this;
    }

    /**
     * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
     * @return The bundlePath.
     */
    @java.lang.Override
    public java.lang.String getBundlePath() {
      return instance.getBundlePath();
    }
    /**
     * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
     * @return The bytes for bundlePath.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getBundlePathBytes() {
      return instance.getBundlePathBytes();
    }
    /**
     * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
     * @param value The bundlePath to set.
     * @return This builder for chaining.
     */
    public Builder setBundlePath(
        java.lang.String value) {
      copyOnWrite();
      instance.setBundlePath(value);
      return this;
    }
    /**
     * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
     * @return This builder for chaining.
     */
    public Builder clearBundlePath() {
      copyOnWrite();
      instance.clearBundlePath();
      return this;
    }
    /**
     * <code>string bundle_path = 2 [json_name = "bundlePath"];</code>
     * @param value The bytes for bundlePath to set.
     * @return This builder for chaining.
     */
    public Builder setBundlePathBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setBundlePathBytes(value);
      return this;
    }

    /**
     * <code>string entry = 3 [json_name = "entry"];</code>
     * @return The entry.
     */
    @java.lang.Override
    public java.lang.String getEntry() {
      return instance.getEntry();
    }
    /**
     * <code>string entry = 3 [json_name = "entry"];</code>
     * @return The bytes for entry.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getEntryBytes() {
      return instance.getEntryBytes();
    }
    /**
     * <code>string entry = 3 [json_name = "entry"];</code>
     * @param value The entry to set.
     * @return This builder for chaining.
     */
    public Builder setEntry(
        java.lang.String value) {
      copyOnWrite();
      instance.setEntry(value);
      return this;
    }
    /**
     * <code>string entry = 3 [json_name = "entry"];</code>
     * @return This builder for chaining.
     */
    public Builder clearEntry() {
      copyOnWrite();
      instance.clearEntry();
      return this;
    }
    /**
     * <code>string entry = 3 [json_name = "entry"];</code>
     * @param value The bytes for entry to set.
     * @return This builder for chaining.
     */
    public Builder setEntryBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setEntryBytes(value);
      return this;
    }

    /**
     * <pre>
     * Where this plugin's own files live: its configuration, unpacked from the
     * bundle's config/ on first load, and anything it writes for itself.
     *
     * The host owns it. It creates the directory, seeds the defaults and never
     * overwrites what an admin edited, so a runtime is told the path rather than
     * deriving one — two runtimes deriving it separately would eventually derive
     * it differently.
     *
     * Added in ABI 1 after the fact: a runtime built before this reads an empty
     * string, which is why nothing may assume it is set.
     * </pre>
     *
     * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
     * @return The dataDirectory.
     */
    @java.lang.Override
    public java.lang.String getDataDirectory() {
      return instance.getDataDirectory();
    }
    /**
     * <pre>
     * Where this plugin's own files live: its configuration, unpacked from the
     * bundle's config/ on first load, and anything it writes for itself.
     *
     * The host owns it. It creates the directory, seeds the defaults and never
     * overwrites what an admin edited, so a runtime is told the path rather than
     * deriving one — two runtimes deriving it separately would eventually derive
     * it differently.
     *
     * Added in ABI 1 after the fact: a runtime built before this reads an empty
     * string, which is why nothing may assume it is set.
     * </pre>
     *
     * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
     * @return The bytes for dataDirectory.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getDataDirectoryBytes() {
      return instance.getDataDirectoryBytes();
    }
    /**
     * <pre>
     * Where this plugin's own files live: its configuration, unpacked from the
     * bundle's config/ on first load, and anything it writes for itself.
     *
     * The host owns it. It creates the directory, seeds the defaults and never
     * overwrites what an admin edited, so a runtime is told the path rather than
     * deriving one — two runtimes deriving it separately would eventually derive
     * it differently.
     *
     * Added in ABI 1 after the fact: a runtime built before this reads an empty
     * string, which is why nothing may assume it is set.
     * </pre>
     *
     * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
     * @param value The dataDirectory to set.
     * @return This builder for chaining.
     */
    public Builder setDataDirectory(
        java.lang.String value) {
      copyOnWrite();
      instance.setDataDirectory(value);
      return this;
    }
    /**
     * <pre>
     * Where this plugin's own files live: its configuration, unpacked from the
     * bundle's config/ on first load, and anything it writes for itself.
     *
     * The host owns it. It creates the directory, seeds the defaults and never
     * overwrites what an admin edited, so a runtime is told the path rather than
     * deriving one — two runtimes deriving it separately would eventually derive
     * it differently.
     *
     * Added in ABI 1 after the fact: a runtime built before this reads an empty
     * string, which is why nothing may assume it is set.
     * </pre>
     *
     * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
     * @return This builder for chaining.
     */
    public Builder clearDataDirectory() {
      copyOnWrite();
      instance.clearDataDirectory();
      return this;
    }
    /**
     * <pre>
     * Where this plugin's own files live: its configuration, unpacked from the
     * bundle's config/ on first load, and anything it writes for itself.
     *
     * The host owns it. It creates the directory, seeds the defaults and never
     * overwrites what an admin edited, so a runtime is told the path rather than
     * deriving one — two runtimes deriving it separately would eventually derive
     * it differently.
     *
     * Added in ABI 1 after the fact: a runtime built before this reads an empty
     * string, which is why nothing may assume it is set.
     * </pre>
     *
     * <code>string data_directory = 4 [json_name = "dataDirectory"];</code>
     * @param value The bytes for dataDirectory to set.
     * @return This builder for chaining.
     */
    public Builder setDataDirectoryBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setDataDirectoryBytes(value);
      return this;
    }

    /**
     * <pre>
     * Where the command tree sits inside the bundle, from the manifest's
     * [commands] tree. Empty when the plugin declares no commands.
     *
     * The path, not the tree. The runtime opens this bundle anyway, and the tree
     * is what its executor ids are numbered by, so sending a copy would be a
     * second definition of it — free to disagree with the one the runtime binds
     * its handlers against. Sending the path instead is what the host already
     * does for entry: it read the manifest, so the runtime does not have to.
     * </pre>
     *
     * <code>string command_tree = 5 [json_name = "commandTree"];</code>
     * @return The commandTree.
     */
    @java.lang.Override
    public java.lang.String getCommandTree() {
      return instance.getCommandTree();
    }
    /**
     * <pre>
     * Where the command tree sits inside the bundle, from the manifest's
     * [commands] tree. Empty when the plugin declares no commands.
     *
     * The path, not the tree. The runtime opens this bundle anyway, and the tree
     * is what its executor ids are numbered by, so sending a copy would be a
     * second definition of it — free to disagree with the one the runtime binds
     * its handlers against. Sending the path instead is what the host already
     * does for entry: it read the manifest, so the runtime does not have to.
     * </pre>
     *
     * <code>string command_tree = 5 [json_name = "commandTree"];</code>
     * @return The bytes for commandTree.
     */
    @java.lang.Override
    public com.google.protobuf.ByteString
        getCommandTreeBytes() {
      return instance.getCommandTreeBytes();
    }
    /**
     * <pre>
     * Where the command tree sits inside the bundle, from the manifest's
     * [commands] tree. Empty when the plugin declares no commands.
     *
     * The path, not the tree. The runtime opens this bundle anyway, and the tree
     * is what its executor ids are numbered by, so sending a copy would be a
     * second definition of it — free to disagree with the one the runtime binds
     * its handlers against. Sending the path instead is what the host already
     * does for entry: it read the manifest, so the runtime does not have to.
     * </pre>
     *
     * <code>string command_tree = 5 [json_name = "commandTree"];</code>
     * @param value The commandTree to set.
     * @return This builder for chaining.
     */
    public Builder setCommandTree(
        java.lang.String value) {
      copyOnWrite();
      instance.setCommandTree(value);
      return this;
    }
    /**
     * <pre>
     * Where the command tree sits inside the bundle, from the manifest's
     * [commands] tree. Empty when the plugin declares no commands.
     *
     * The path, not the tree. The runtime opens this bundle anyway, and the tree
     * is what its executor ids are numbered by, so sending a copy would be a
     * second definition of it — free to disagree with the one the runtime binds
     * its handlers against. Sending the path instead is what the host already
     * does for entry: it read the manifest, so the runtime does not have to.
     * </pre>
     *
     * <code>string command_tree = 5 [json_name = "commandTree"];</code>
     * @return This builder for chaining.
     */
    public Builder clearCommandTree() {
      copyOnWrite();
      instance.clearCommandTree();
      return this;
    }
    /**
     * <pre>
     * Where the command tree sits inside the bundle, from the manifest's
     * [commands] tree. Empty when the plugin declares no commands.
     *
     * The path, not the tree. The runtime opens this bundle anyway, and the tree
     * is what its executor ids are numbered by, so sending a copy would be a
     * second definition of it — free to disagree with the one the runtime binds
     * its handlers against. Sending the path instead is what the host already
     * does for entry: it read the manifest, so the runtime does not have to.
     * </pre>
     *
     * <code>string command_tree = 5 [json_name = "commandTree"];</code>
     * @param value The bytes for commandTree to set.
     * @return This builder for chaining.
     */
    public Builder setCommandTreeBytes(
        com.google.protobuf.ByteString value) {
      copyOnWrite();
      instance.setCommandTreeBytes(value);
      return this;
    }

    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    @java.lang.Override
    public java.util.List<fr.gocraft.abi.v1.EventBinding> getEventTypesList() {
      return java.util.Collections.unmodifiableList(
          instance.getEventTypesList());
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    @java.lang.Override
    public int getEventTypesCount() {
      return instance.getEventTypesCount();
    }/**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    @java.lang.Override
    public fr.gocraft.abi.v1.EventBinding getEventTypes(int index) {
      return instance.getEventTypes(index);
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder setEventTypes(
        int index, fr.gocraft.abi.v1.EventBinding value) {
      copyOnWrite();
      instance.setEventTypes(index, value);
      return this;
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder setEventTypes(
        int index, fr.gocraft.abi.v1.EventBinding.Builder builderForValue) {
      copyOnWrite();
      instance.setEventTypes(index,
          builderForValue.build());
      return this;
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder addEventTypes(fr.gocraft.abi.v1.EventBinding value) {
      copyOnWrite();
      instance.addEventTypes(value);
      return this;
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder addEventTypes(
        int index, fr.gocraft.abi.v1.EventBinding value) {
      copyOnWrite();
      instance.addEventTypes(index, value);
      return this;
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder addEventTypes(
        fr.gocraft.abi.v1.EventBinding.Builder builderForValue) {
      copyOnWrite();
      instance.addEventTypes(builderForValue.build());
      return this;
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder addEventTypes(
        int index, fr.gocraft.abi.v1.EventBinding.Builder builderForValue) {
      copyOnWrite();
      instance.addEventTypes(index,
          builderForValue.build());
      return this;
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder addAllEventTypes(
        java.lang.Iterable<? extends fr.gocraft.abi.v1.EventBinding> values) {
      copyOnWrite();
      instance.addAllEventTypes(values);
      return this;
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder clearEventTypes() {
      copyOnWrite();
      instance.clearEventTypes();
      return this;
    }
    /**
     * <pre>
     * The plugin-defined event types this plugin provides or subscribes to, with
     * the id the host assigned each of them. Empty for a plugin that touches
     * none, which is every plugin until one declares [[events.provides]].
     *
     * Sent per plugin rather than once per runtime, because a runtime has no use
     * for the id of an event none of its plugins can see, and because the host
     * assigns the ids from the full set of installed manifests — a fact that is
     * only settled once every bundle has been scanned.
     * </pre>
     *
     * <code>repeated .gocraft.abi.v1.EventBinding event_types = 6 [json_name = "eventTypes"];</code>
     */
    public Builder removeEventTypes(int index) {
      copyOnWrite();
      instance.removeEventTypes(index);
      return this;
    }

    // @@protoc_insertion_point(builder_scope:gocraft.abi.v1.Load)
  }
  @java.lang.Override
  @java.lang.SuppressWarnings({"ThrowNull"})
  protected final java.lang.Object dynamicMethod(
      com.google.protobuf.GeneratedMessageLite.MethodToInvoke method,
      java.lang.Object arg0, java.lang.Object arg1) {
    switch (method) {
      case NEW_MUTABLE_INSTANCE: {
        return new fr.gocraft.abi.v1.Load();
      }
      case NEW_BUILDER: {
        return new Builder();
      }
      case BUILD_MESSAGE_INFO: {
          java.lang.Object[] objects = new java.lang.Object[] {
            "pluginId_",
            "bundlePath_",
            "entry_",
            "dataDirectory_",
            "commandTree_",
            "eventTypes_",
            fr.gocraft.abi.v1.EventBinding.class,
          };
          java.lang.String info =
              "\u0000\u0006\u0000\u0000\u0001\u0006\u0006\u0000\u0001\u0000\u0001\u0208\u0002\u0208" +
              "\u0003\u0208\u0004\u0208\u0005\u0208\u0006\u001b";
          return newMessageInfo(DEFAULT_INSTANCE, info, objects);
      }
      case GET_DEFAULT_INSTANCE: {
        return DEFAULT_INSTANCE;
      }
      case GET_PARSER: {
        com.google.protobuf.Parser<fr.gocraft.abi.v1.Load> parser = PARSER;
        if (parser == null) {
          synchronized (fr.gocraft.abi.v1.Load.class) {
            parser = PARSER;
            if (parser == null) {
              parser =
                  new DefaultInstanceBasedParser<fr.gocraft.abi.v1.Load>(
                      DEFAULT_INSTANCE);
              PARSER = parser;
            }
          }
        }
        return parser;
      }
      case GET_MEMOIZED_IS_INITIALIZED: {
        return (byte) 1;
      }
      // SET_MEMOIZED_IS_INITIALIZED is never called for this message.
      // So it can do anything. Combine with default case for smaller codegen.
      case SET_MEMOIZED_IS_INITIALIZED:
    }
    // Should never happen. Generates tight code to throw an exception.
    throw null;
  }


  // @@protoc_insertion_point(class_scope:gocraft.abi.v1.Load)
  private static final fr.gocraft.abi.v1.Load DEFAULT_INSTANCE;
  static {
    Load defaultInstance = new Load();
    // New instances are implicitly immutable so no need to make
    // immutable.
    DEFAULT_INSTANCE = defaultInstance;
    com.google.protobuf.GeneratedMessageLite.registerDefaultInstance(
      Load.class, defaultInstance);
  }

  public static fr.gocraft.abi.v1.Load getDefaultInstance() {
    return DEFAULT_INSTANCE;
  }

  private static volatile com.google.protobuf.Parser<Load> PARSER;

  public static com.google.protobuf.Parser<Load> parser() {
    return DEFAULT_INSTANCE.getParserForType();
  }
}

